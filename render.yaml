services:
  - type: web
    name: sphere-dev-network
    env: node
    plan: free
    buildCommand: npm run install-all && npm run render-build
    startCommand: npm run render-start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
